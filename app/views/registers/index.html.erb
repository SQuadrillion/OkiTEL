<div class="container-fluid">
  <% Register.all.each do |r| %>
  <a class="row list" href="show/<%= r.id %>">
    <div class="col-xs-12">
      <div class="text-center">
        <div class="col-xs-4">
          <div style="font-size:1.5em; line-height: 5em;">00:30</div>
        </div>
        <div class="col-xs-8">
          <%= r.name %>さん<br><%= r.todo %>
        </div>
      </div>
    </div>
  </a>
  <% end %>
</div>

